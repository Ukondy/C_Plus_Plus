#include <string>
#include <iostream>
#include <array>
#include <windows.h>

#define FOO(x,y)((x)-(y))
#define moreThan(x,y)((x) > (y))
#define avg(a,b,c,d,e)(((a) + (b) + (c) + (d)) / (e))

using namespace std;

#if 0
int main(int argc, char* argv[]) {
	setlocale(LC_ALL, "en");

	return 1;
}
#endif 